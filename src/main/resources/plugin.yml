version: ${project.version}
api-version: ${mc.version}
build-date: ${build.date}
build-number: ${build.number}

main: com.groupId.artifactId.Main
name: __NAME__
author: __AUTHOR__

commands:
  # commands

permissions:
  # permissions
